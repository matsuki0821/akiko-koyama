---
// ヒーロー画像の表示位置（%）。左0〜右100、上0〜下100。
// 横位置が効きにくい場合のために、微小なズームと平行移動も用意しています。
const HERO_OBJECT_POS_X = 55;  // 横位置（左0〜右100）
const HERO_OBJECT_POS_Y = 80; // 縦位置（100%で下寄せ）
const HERO_SHIFT_X = -6;       // 左へ6%シフト
const HERO_SCALE = 1.12;       // 右端に余白が出ないように拡大
// 見出しブロックの上下シフト量（px）。負の値で上へ、正の値で下へ。
const HERO_TEXT_SHIFT_Y = -80;
---

<section class="relative h-svh min-h-[800px] flex flex-col justify-center bg-gradient-to-b from-white to-[#f7f6f4] overflow-hidden">
  <img
    aria-hidden="true"
    src="/images/aki.jpg"
    alt=""
    class="absolute inset-0 h-full w-full object-cover hero-img"
    loading="lazy"
    decoding="async"
  />
  <div
    class="mx-auto w-full max-w-[1600px] max-h-[1000px] relative z-10 flex flex-col items-center justify-center px-6 py-16"
  >
    

    <div class="relative z-10 flex flex-col items-center text-center gap-6" style={`transform: translateY(${HERO_TEXT_SHIFT_Y}px);`}>
      <!-- 認定プログラムバッジを非表示（削除） -->

      <!-- モバイル版: 3行表示 -->
      <h1 class="sm:hidden text-4xl font-extrabold leading-tight tracking-tight text-base-900">
        <span class="block">病気じゃない。</span>
        <span class="block">でも苦しい。</span>
        <span class="block">だからここで <span class="text-primary">学ぶ</span>。</span>
      </h1>

      <!-- デスクトップ版: 既存の2行表示を維持 -->
      <h1 class="hidden sm:block text-4xl sm:text-6xl md:text-7xl font-extrabold leading-tight tracking-tight text-base-900">
        <span class="block">病気じゃない。でも苦しい。</span>
        <span class="block">だからここで <span class="text-primary">学ぶ</span>。</span>
      </h1>


      <!-- 「ビデオを見る」を一旦非表示（削除） -->

      <!-- 動画モーダルとスクリプトを一旦非表示（削除） -->

      <!-- CTAボタン（登校サポートを見る）を非表示 -->
    </div>
  </div>

  
 </section>

<style>
  /* デスクトップの既定位置（以前の定数: X=55%, Y=80%, shiftX=-6%, scale=1.12） */
  .hero-img {
    object-position: 55% 80%;
    transform: translateX(-6%) scale(1.12);
  }
  /* モバイルでは講師写真が左に寄りすぎるため右寄せに補正 */
  @media (max-width: 640px) {
    .hero-img {
      /* まずは中央から少し右・やや上へ */
      object-position: 40% 40%;
      transform: translateX(0) translateY(-6%) scale(1.08);
    }
  }
</style>

